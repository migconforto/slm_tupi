# Núcleo de treino / inferência (SFT com LoRA sobre Qwen2.5-3B-Instruct)
# Testado com Python 3.10 e CUDA 12.1 (torch 2.5.1+cu121).

torch>=2.5.0
transformers>=4.44.0
datasets>=2.20.0
peft>=0.12.0
trl>=0.9.0
accelerate>=0.33.0
bitsandbytes>=0.43.0        # quantização 4-bit (opcional / Linux)
tensorboard>=2.16.0
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0            # exportar predictions.xlsx
tqdm>=4.66.0

langchain>=0.2.0           # localLLM_running_inference.py